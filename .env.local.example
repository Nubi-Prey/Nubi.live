# Personal Access Token do GitHub (escopo de leitura pública) usado para
# consultar api.github.com/graphql em getStaticProps.
# Copie este arquivo para ".env.local" (gitignored) e preencha o valor.
# Em produção, configure a mesma variável nas Environment Variables do projeto na Vercel.
GITHUB_TOKEN=
